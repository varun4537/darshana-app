/**
 * Centralised localStorage key constants.
 * Import these instead of using magic strings scattered across files.
 */
export const STORAGE_KEYS = {
    USER_STATS: 'darshana_user_stats',
    IS_PREMIUM: 'darshana_is_premium',
} as const;
